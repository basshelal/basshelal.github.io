import React, {FC} from "react"
import {P} from "../../Utils"

// TODO: We need to implement a search functionality
//  that would include search by tags 1 or more both being a union and being distinct
//  like posts that have tag A AND B or posts that have tag A OR B
//  search by word in title or subtitle
//  search should use query params in url
export const BlogPage: FC = (props: P) => {
    return (<>
        Blog
    </>)
}