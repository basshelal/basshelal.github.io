import * as React from "react"
import {FC} from "react"

export interface BlogPost {
    name: string,
    url: string,
    date: Date
    // blurb?
    // image?
    // read time?
    // tags?
}

export const BlogView: FC = () => {
    // Blog Title
    // Date
    // Table of Contents (from the headings)
    // Content (can contain headings & subheadings, images and code blocks)
    // Previous Post
    // Next Post

    return (<>


    </>)
}