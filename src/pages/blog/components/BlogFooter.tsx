import React, {PropsWithChildren} from "react"
import {Paper} from "@mui/material"

export const BlogFooter = (props: PropsWithChildren<{}>) => {
    return (<>
        <Paper square elevation={0} style={{backgroundColor: "#880E4F", marginTop: "16px"}}>
        </Paper>
    </>)
}