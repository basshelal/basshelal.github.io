export const _ = null

// For each file in ./posts get its readable name by replacing - with space and removing the .tsx extension
// The list can contain all of the posts, we don't need pages yet
// The list is just links to the actual posts